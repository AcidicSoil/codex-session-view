# .SESSION-EXPLORER_DEBUB_TODOS

- **ordered by highest priority first to last**
**all need to be (mouse)clickable to expand and shrink**
// TODO (fix)session_explorer

- requires refresh before items appear
  - dissappears when triggered with mouse click
    - requires refresh before items appear

// TODOS (fix)session_explorer

## BUTTONS

-

## CHIPS AKA filter button components on session_explorer

- add in drop down component to house chips
- adjust size & date filter chip values
